{
  "name": "wisizer",
  "version": "1.0.0",
  "private": true,
  "description": "Whistle & Flute hole position calculator",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage tests/*.test.js"
  }
}
